name: Audit

on:
  pull_request:
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  audit:
    permissions:
      contents: read
    uses: SC0RED/clawndom/.github/workflows/audit-agent-workspace.yml@main
    secrets: inherit
    with:
      workspaces: |
        workspaces/patch
        workspaces/scarlett
      shared_dir: workspaces/shared
